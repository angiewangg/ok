# Mini Fortnite Demo

Single-player demo showing basic movement, shooting, enemies, and a shrinking safe zone.

Open `index.html` in a browser or run a local server in the `games` folder:

```
python -m http.server 8000
```

Controls: WASD to move, mouse to aim, click to shoot.
