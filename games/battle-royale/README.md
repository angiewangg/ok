# Battle-Royale Prototype

This folder contains a Fortnite-inspired Battle Royale boarding screen and battle bus sequence prototype.

Open `index.html` in a browser or serve the folder locally:

```
cd games/battle-royale
python -m http.server 8000
```

Controls:
- Space: after boarding, press to jump (advances to parachute sequence in next step)
